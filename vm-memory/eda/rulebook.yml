---
- name: Run a webhook listener service
  hosts: all
  sources:
    - ansible.eda.webhook:
        port: 5000
  rules:
    - name: Webhook event
      condition: event.payload.status == 'firing' 
      action:
        debug:
          msg: "Webhook triggered! Event: {{ event.payload }}"
